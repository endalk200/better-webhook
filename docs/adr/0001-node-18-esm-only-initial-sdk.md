# Node 18 and ESM-only initial SDK

The initial SDK packages target Node.js 18+ and publish ESM-only packages with TypeScript declarations. Edge runtimes, browser usage, and CommonJS compatibility are deferred so the first release can focus on correct raw-body signature verification, provider semantics, framework adapters, idempotency, replay protection, and observability without multiplying runtime and package-format edge cases.

name: Build CLI Binaries

on:
  push:
    tags:
      - "@better-webhook/cli@*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g. @better-webhook/cli@3.7.0)"
        required: true
        type: string

# Ensure we don't run multiple binary builds concurrently for the same tag
concurrency:
  group: binary-release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  build-binaries:
    name: Build ${{ matrix.artifact }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: bun-linux-x64
            os: ubuntu-latest
            artifact: better-webhook-linux-x64
          - target: bun-linux-arm64
            os: ubuntu-24.04-arm # Native ARM64 runner
            artifact: better-webhook-linux-arm64
          - target: bun-darwin-x64
            os: macos-latest
            artifact: better-webhook-darwin-x64
          - target: bun-darwin-arm64
            os: macos-latest
            artifact: better-webhook-darwin-arm64
          - target: bun-windows-x64
            os: windows-latest
            artifact: better-webhook-windows-x64.exe

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_TAG }}
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build dashboard
        run: pnpm --filter @better-webhook/dashboard build

      - name: Build CLI
        run: pnpm --filter @better-webhook/cli build:cli

      - name: Extract version from tag
        id: version
        shell: bash
        run: |
          if [[ "${RELEASE_TAG}" != @better-webhook/cli@* ]]; then
            echo "Invalid tag: ${RELEASE_TAG}"
            exit 1
          fi
          VERSION="${RELEASE_TAG#@better-webhook/cli@}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Compile binary
        working-directory: apps/webhook-cli
        shell: bash
        run: |
          bun ./scripts/build-binary.ts --target ${{ matrix.target }}
        env:
          CLI_VERSION: ${{ steps.version.outputs.version }}

      - name: Trivy binary gate
        if: ${{ vars.TRIVY_ENFORCE == '1' }}
        uses: aquasecurity/trivy-action@c1824fd6edce30d7ab345a9989de00bbd46ef284
        with:
          scan-type: fs
          scan-ref: .
          cache: ${{ runner.os != 'Windows' }}
          trivy-config: trivy.yaml
          version: 0.69.0
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1

      - name: Trivy binary report
        if: ${{ vars.TRIVY_ENFORCE != '1' }}
        uses: aquasecurity/trivy-action@c1824fd6edce30d7ab345a9989de00bbd46ef284
        with:
          scan-type: fs
          scan-ref: .
          cache: ${{ runner.os != 'Windows' }}
          trivy-config: trivy.yaml
          version: 0.69.0
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 0

      - name: Generate binary SBOM
        uses: aquasecurity/trivy-action@c1824fd6edce30d7ab345a9989de00bbd46ef284
        with:
          scan-type: fs
          scan-ref: .
          cache: ${{ runner.os != 'Windows' }}
          trivy-config: trivy.yaml
          format: cyclonedx
          output: apps/webhook-cli/bin/${{ matrix.artifact }}.sbom.cdx.json
          scanners: vuln
          exit-code: 0
          skip-setup-trivy: true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            apps/webhook-cli/bin/${{ matrix.artifact }}
            apps/webhook-cli/bin/${{ matrix.artifact }}.sbom.cdx.json
          if-no-files-found: error

  release:
    name: Upload to Release
    needs: build-binaries
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        working-directory: artifacts
        run: |
          sha256sum better-webhook-* > checksums.txt
          echo "Generated checksums:"
          cat checksums.txt

      - name: Extract version from tag
        id: version
        run: |
          if [[ "${RELEASE_TAG}" != @better-webhook/cli@* ]]; then
            echo "Invalid tag: ${RELEASE_TAG}"
            exit 1
          fi
          VERSION="${RELEASE_TAG#@better-webhook/cli@}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: "@better-webhook/cli v${{ steps.version.outputs.version }}"
          generate_release_notes: true
          draft: false
          prerelease: false
          files: |
            artifacts/better-webhook-*
            artifacts/checksums.txt
          fail_on_unmatched_files: true

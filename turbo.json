{
  "$schema": "https://v2-8-13.turborepo.dev/schema.json",
  "ui": "stream",
  "globalEnv": [
    "WEBHOOK_SECRET",
    "GITHUB_WEBHOOK_SECRET",
    "STRIPE_WEBHOOK_SECRET",
    "RAGIE_WEBHOOK_SECRET",
    "RECALL_WEBHOOK_SECRET",
    "RESEND_WEBHOOK_SECRET",
    "DATABASE_URL",
    "NODE_ENV",
    "PORT",
    "BETTER_WEBHOOK_ENDPOINT_IDENTITY",
    "BETTER_WEBHOOK_IDEMPOTENCY_TTL_MS",
    "BETTER_WEBHOOK_OTEL_ENDPOINT",
    "BETTER_WEBHOOK_REPLAY_WINDOW_MS",
    "BETTER_WEBHOOK_URL",
    "OTEL_SERVICE_NAME"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [
        "dist/**",
        "build/**",
        "bin/**",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "lint": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "eslint.config.*"]
    },
    "check-types": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "tsconfig*.json"]
    },
    "test": {
      "dependsOn": ["^build", "^test"],
      "env": ["TEST_WEBHOOK_SECRET"],
      "inputs": ["$TURBO_DEFAULT$", "jest.config.*", "vitest.config.*"]
    },
    "format:check": {
      "inputs": ["$TURBO_DEFAULT$"]
    },
    "format:write": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false,
      "outputs": ["dist/**", "build/**", ".turbo/**"]
    }
  }
}

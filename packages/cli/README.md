# @better-webhook/cli

Node.js wrapper package for the native `better-webhook` CLI binary.

The package installs a platform-specific binary package and exposes the `better-webhook` command in your PATH.
